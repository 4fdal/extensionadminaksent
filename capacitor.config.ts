import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'extension-admin-aksent',
  webDir: 'dist'
};

export default config;
